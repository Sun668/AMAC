# AMAC P0 robustness analysis

This experiment reads sealed CH-SIMS v2 predictions and decisions. It does not
retrain a perception or risk model and does not replace the primary result.

Run `freeze_snapshot.py`, `run_analysis.py`, `validate_analysis.py`, then
`render_paper_tables.py`. Every run uses a new absent result directory.

