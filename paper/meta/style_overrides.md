# Style overrides

- Use American English.
- Use `multimodal affective agent`, not `emotion AI`.
- Use `commitment contract` for the state machine and `risk estimator` for the learned score.
- Reserve `H0` for the preregistered MLP condition and `LR` for the selected logistic estimator.
- Do not call the author-defined protocol an official CH-SIMS v2 benchmark.
- Do not claim that AMAC improves terminal TAV recognition.
- Do not claim universal revision reduction.
- Do not describe in-process tool latency as end-to-end latency.
- Avoid architecture novelty claims because LR is non-inferior to H0.

